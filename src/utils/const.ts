export enum AuthState {
    Loading = 'loading',
    LoggedIn = 'loggedIn',
    LoggedOut = 'loggedOut',
}

export enum Routes {
    Login = '/login',
    Dashboard = '/dashboard',
    Redirect = '/',
}
